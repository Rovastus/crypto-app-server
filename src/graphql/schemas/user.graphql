extend type Query {
  login(email: String!, password: String!): AuthenticateResponse!
}

extend type Mutation {
  registers(email: String!, password: String!): AuthenticateResponse!
}

type AuthenticateResponse {
  token: String!
}
